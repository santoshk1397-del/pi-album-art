node_modules/
dist/
.DS_Store
*.local

# Dead credential files from the removed Python tooling. Kept ignored so they
# can never be committed, even though nothing reads them any more.
config.json
bridge/bridge-config.json
__pycache__/
*.pyc

# The 2.9MB source PNG is unused - the app loads the optimised .jpg. Git keeps
# every version of a binary forever, so keep the big one out.
web/public/not_playing.png
